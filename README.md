#To deploy new production to vercel. fist commit and push code to github, merge code with main branch.

# cd into the client folder (if created- not at the moment) and run the command "now" in your terminal

# after it is finished building run the next command "vercel --prod". After this step is complete the web-app should be up and running on the production url
